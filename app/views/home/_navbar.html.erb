  <nav class="flex items-center justify-between mb-8">
    <h1 class="text-4xl uppercase font-extrabold text-gray-900 hover:italic">
      <%= link_to "Logo"%>
    <h1>
    <div class="flex gap-6">
      <% if signed_in? %>
        <div class="bg-orange-500 p-2 rounded-md capitalize font-bold cursor-pointer text-gray-200 hover:bg-orange-600">
          <%= link_to destroy_user_session_path, data: {turbo_method: :delete} do%>
            <span class="flex items-center cursor-pointer gap-2">Logout <%= inline_svg_tag("arrow-right-circle.svg", class:"size-6")%></span>
          <%end%>
        </div>
        <div class="bg-gray-800 p-2 rounded-md capitalize cursor-pointer font-bold text-gray-200 hover:bg-gray-600">
          <%= link_to "Dashboard", dashboard_index_path %>
        <div>
      <% else %>
        <div class="bg-orange-500 p-2 rounded-md cursor-pointer capitalize font-bold text-gray-200 hover:bg-orange-600">
          <%= link_to new_user_session_path do%>
            <span class="flex items-center gap-2 cursor-pointer">Login <%= inline_svg_tag("arrow-right-circle.svg", class:"size-6")%></span>
          <%end%>
        </div>
        <div class="bg-gray-800 p-2 rounded-md cursor-pointer capitalize font-bold text-gray-200 hover:bg-gray-600">
          <%= link_to "Signup", new_user_registration_path %>
        <div>
      <% end %>
      </div>
    </div>
  </nav>